# 画面：復習

**目的：** 問題を解いて正誤を記録し、正解時は次の復習日の設定・克服済み化、不正解時はメモ・復習日の更新を行う。

---

## 操作フロー

1. 問題文を表示（正解は非表示）
2. 自分の答えを入力（テキストエリア、**任意**。書かずに次へ進んでもよい）
3. 「答えを見る」→ 正解を表示
4. 正解 / 不正解 を選択（自己採点。attempt として記録される）
   - **正解** → 次回設定フォームを展開
     - レスポンスの `mastery_suggested` が true の場合、「3回連続で正解しています。克服済みにしますか？」の提案を表示（克服ボタンを強調）
     - 次の復習日を設定して保存 → ホームに戻る
     - または「克服済みにする」→ その場で mastered に変更 → ホームに戻る（提案の有無にかかわらず手動で克服できる）
   - **不正解** → メモ編集フォームを展開
5. メモ・復習日を入力して保存 → ホームに戻る

---

## 表示データ

| 項目 | 表示タイミング |
|------|-------------|
| 問題文（question_text） | 常時 |
| 科目名 / 単元名 | 常時 |
| 間違えた回数（wrong_count）／連続正解回数（correct_streak） | 常時 |
| 正解（correct_answer） | 「答えを見る」押下後 |
| 間違えた理由（memo） | 「答えを見る」押下後（未設定の場合は非表示） |
| 今回学んだこと（learning） | 「答えを見る」押下後（未設定の場合は非表示） |
| 次の復習日（next_review_at）設定フォーム | 正解選択後 |
| 「克服済みにする」ボタン | 正解選択後（`mastery_suggested` 時は提案バナーで強調） |
| メモ・復習日 編集フォーム | 不正解選択後 |

---

## 使用API

- `GET /mistake-notes/{id}`（URL直接アクセス・リロード時のデータ取得）
- `POST /questions/{id}/attempts`（正解/不正解と任意の user_answer を記録。レスポンスに correct_streak / mastery_suggested）
- `PUT /mistake-notes/{id}`（メモ・復習日を更新）
- `PUT /mistake-notes/{id}/status`（正解時：「克服済みにする」で mastered 化）

---

## 関連ドキュメント

- [画面遷移図](./transitions.md)
- [API: 回答を記録](../api/attempts.md)
- [API: 間違いノート ステータス変更](../api/mistake-notes.md)
- [DB: correct_streak ルール](../db/design.md)
- [共通UI仕様](./common-ui.md)
